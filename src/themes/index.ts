import defaultTheme from "./defaultTheme";
import * as styles from "./styles";
import ThemeProvider from "./ThemeProvider";
import * as types from "./types";

export { defaultTheme, styles, ThemeProvider, types };

export default {
    defaultTheme,
    styles,
    ThemeProvider,
    types,
};
